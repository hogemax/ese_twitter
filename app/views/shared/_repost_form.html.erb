<% if @micropost %>
  <%= form_for(@micropost) do |f| %>
    <%= render 'shared/error_messages', object: f.object %>
    <div class="field">
      <%= f.text_area :content, placeholder: "新しい投稿内容を入力..." %>
    </div>

    <div class="well">
      <%= label :post_source, :content, :id =>"repost_source_content" %>
      <%= image_tag '', :id => 'repost_source_image' %>
      <%= hidden_field :post_source, :id, :id =>"repost_source_id" %>
    </div>

    <%= f.submit "投稿する", class: "btn btn-lg btn-primary postButtonDefault" %>
  <% end %>
<% end %>
